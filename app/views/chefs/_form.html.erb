<%= render 'shared/header' do %>
 <%= link_to '< Back', chefs_path , :style=>"color: #476072;"%>
<% end %>

<div class="container py-5">
  <h3 class="text-center">New Chef</h3>
  <div class="row">
    <div class="col-12 col-md-8 offset-md-2 col-lg-6 offset-md-3">
      <div class="form-container">
        <%= simple_form_for (chef) do |f| %>
          <%= f.input :category %>
          <%= f.input :name %>
          <%= f.input :description %>
          <%= f.input :price %>
          <%= f.submit class: 'btn btn-ghost' %>
        <% end %>
      </div>
    </div>
        <%# <%= link_to 'Back', chefs_path %>
  </div>
</div>
