<div class="row" style="margin-left: 100px; margin-top: 50px;" >
  <%= link_to '< Back', booking_path(@booking) , :style=>"color: #476072;"%>
</div>

<div class="container py-5">
  <h3 class="text-center">New Review</h3>
  <div class="row">
    <div class="col-12 col-md-6 offset-lg-3">
      <div class="bg-white rounded p-3 shadow">
        <%= simple_form_for [@booking.chef, @booking, @review] do |f| %>
          <%= f.input :rating %>
          <%= f.input :content %>
          <%= f.submit class: 'btn btn-ghost' %>
        <% end %>
    <%# <%= link_to '< Back', booking_path(@booking) %>
</div>
